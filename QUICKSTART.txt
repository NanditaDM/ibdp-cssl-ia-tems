T.E.A.Ms.M - QUICK START GUIDE
IBDP CS SL Internal Assessment

=== GETTING STARTED ===

1. Open login.html in any web browser
   - Double-click login.html, or
   - Right-click -> Open With -> Browser

2. Login with test credentials:
   Employee: carol@company.com / emp123
   Manager: alice@company.com / mgr123
   HR: hr@company.com / hr123
   IT: it@company.com / it123
   CEO: ceo@company.com / ceo123

3. Explore the dashboard features based on your role

=== TESTING WORKFLOW ===

Test as Employee (carol@company.com / emp123):
1. Log Attendance -> Enter late reason "Traffic" -> Submit
2. Request Leave -> Select type and fill details -> Submit
3. Open IT Ticket -> Select issue type -> Submit
4. Report to HR -> Fill offender details -> Submit
5. Log Client (Sales only) -> Enter client info -> Submit
6. Log Meeting (Sales only) -> Enter meeting details -> Submit
7. Log Break -> Start Break -> End Break
8. View History -> See all your activities
9. View Notifications -> See messages from manager

Test as Manager (alice@company.com / mgr123):
1. Do all employee actions above
2. View Flagged -> See flagged employees
3. View Requests -> Approve/deny leave requests
4. View Tickets -> See team tickets
5. Send Notification -> Message to team

Test as HR (hr@company.com / hr123):
1. View Reports -> See all behavioral reports
2. Respond to reports -> Enter action taken
3. Send Notification -> Message employees

Test as IT (it@company.com / it123):
1. View Tickets -> See all IT tickets
2. Set ETA for tickets -> Enter estimated time
3. Mark tickets as Resolved

Test as CEO (ceo@company.com / ceo123):
1. View Stats -> See company-wide statistics
2. Send Notification -> Message anyone

=== IMPORTANT FEATURES TO TEST ===

Auto-flagging:
- Login as employee and log attendance with late reason 4 times
- Manager should see employee flagged

Further action on reports:
- Submit HR report as employee
- Login as HR and respond to report
- Login back as employee, view history, request further action
- Login as HR to see further action request

Ticket resolution:
- Submit IT ticket as employee
- Login as IT, set ETA and resolve ticket
- Login back as employee, view history, mark as unresolved if needed

Meeting notifications:
- Login as sales employee
- Log out-of-office meeting
- Login as manager to see notification

=== FILES OVERVIEW ===

User.js - User classes (CEO, HR, IT, Manager, Employee)
Record.js - Record classes (Request, Ticket, Report, etc.)
app.js - Main application logic and data management
dashboard.js - Dashboard UI and event handlers
login.html - Login interface
dashboard.html - Main dashboard interface
test.html - Automated test page

=== NOTES ===

- Data resets on page refresh (in-memory storage)
- All times are captured automatically
- Pagination shows 10 items per page in history
- All OOP concepts implemented (Encapsulation, Inheritance, Polymorphism)
- Code written at beginner level as required
